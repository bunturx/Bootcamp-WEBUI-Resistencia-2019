const defaultState = []; // un arregle de objetos de sugerencias

function reducer (state= defaultState,{type, payload}) {
    switch (type){
        default:
            return state;
    }
}

export default reducer;